class Combatant():
    def __init__(self, actions: str, initiative: int, between: int, speed: int) -> None:
	    actions = self.actions
	    initiative = self.initiative
	    between = self.between
	    speed = self.speed
